% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module.R
\name{mymodule_ui}
\alias{mymodule_ui}
\alias{mymodule_server}
\title{My module}
\usage{
mymodule_ui(
  id,
  group_vars = NULL,
  title = "My module",
  icon = bsicons::bs_icon("bar-chart-line"),
  tooltip = NULL,
  opts_btn_lab = "Options",
  groups_lab = "Group data by",
  no_grouping_lab = "No grouping",
  full_screen = TRUE,
  use_sidebar = FALSE,
  sidebar_title = NULL,
  sidebar_width = 250
)

mymodule_server(
  id,
  df,
  group_vars = NULL,
  group_pal = epi_pals()$frost,
  na_colour = "#666666",
  place_filter = shiny::reactiveVal(),
  time_filter = shiny::reactiveVal(),
  filter_info = shiny::reactiveVal(),
  filter_reset = shiny::reactiveVal()
)
}
\arguments{
\item{id}{Module id. Must match between \code{\link[=mymodule_ui]{mymodule_ui()}} and
\code{\link[=mymodule_server]{mymodule_server()}}.}

\item{group_vars}{Optional named character vector of categorical variables
for an in-card grouping select. Names are used as labels.}

\item{title}{Header title for the card.}

\item{icon}{Icon shown next to the title. Defaults to a \link{bsicons} glyph.}

\item{tooltip}{Optional hover-text shown next to the title.}

\item{opts_btn_lab}{Label for the options popover/sidebar trigger.}

\item{groups_lab}{Label for the grouping select input.}

\item{no_grouping_lab}{Label for the "no grouping" choice.}

\item{full_screen}{Add a full-screen toggle to the card?}

\item{use_sidebar}{If \code{TRUE}, render options in a right-hand \link[bslib:sidebar]{bslib::sidebar}
instead of a popover. Default \code{FALSE}.}

\item{sidebar_title}{Title for the sidebar. Only used when
\code{use_sidebar = TRUE}.}

\item{sidebar_width}{Width of the sidebar in pixels. Default \code{250}.}

\item{df}{A data frame (or \code{\link[shiny:reactive]{shiny::reactive()}} returning one) of patient-
level or aggregated data.}

\item{group_pal}{Categorical colour palette. Defaults to the same \code{frost}
palette used by \code{epishiny}.}

\item{na_colour}{Colour for \code{NA} group level.}

\item{place_filter}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when set, narrows \code{df}
to a clicked admin region. Receives values produced by
\code{epishiny::place_server()}.}

\item{time_filter}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when set, narrows \code{df} to
a clicked time period. Receives values produced by
\code{epishiny::time_server()}.}

\item{filter_info}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} holding the current filter-info
HTML string, kept in sync across modules.}

\item{filter_reset}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when bumped, signals
modules to clear their internal click filters.}
}
\value{
\code{mymodule_ui()} returns a \link[bslib:card]{bslib::card}. \code{mymodule_server()} returns
a \code{\link[shiny:reactive]{shiny::reactive()}} of click-event data, suitable for piping back into
sibling modules' \verb{*_filter} arguments.
}
\description{
One-line description of what this module visualises.
}
\examples{
# Standalone launch of the placeholder module against epishiny's bundled
# example linelist. Replace `mymodule_*` with your renamed functions.
if (interactive()) {
  library(shiny)
  library(bslib)
  library(epishiny.template)

  data("df_ll", package = "epishiny")

  ui <- bslib::page_fillable(
    padding = 0,
    mymodule_ui(
      id = "demo",
      title = "My module",
      group_vars = c("Sex" = "sex_id")
    )
  )

  server <- function(input, output, session) {
    mymodule_server(
      id = "demo",
      df = df_ll,
      group_vars = c("Sex" = "sex_id")
    )
  }

  shiny::shinyApp(ui, server)
}
}
