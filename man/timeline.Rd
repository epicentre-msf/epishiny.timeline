% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epishiny_timeline.R
\name{timeline_ui}
\alias{timeline_ui}
\alias{timeline_server}
\title{Patient care-pathway timeline module}
\usage{
timeline_ui(
  id,
  title = "Timeline",
  icon = bsicons::bs_icon("activity"),
  tooltip = NULL,
  facility_lab = "Health facility",
  cases_lab = "Cases to display",
  label_lab = "Identifier",
  agesex_lab = "Show age / sex",
  incubation_lab = "Assumed incubation (days)",
  incubation_days = 8,
  incubation_min = 0,
  incubation_max = 21,
  opts_btn_lab = "Options",
  full_screen = TRUE,
  use_sidebar = TRUE,
  sidebar_title = NULL,
  sidebar_width = 300
)

timeline_server(
  id,
  df,
  id_var = "patient_name",
  name_var = "patient_name",
  pid_var = "pid",
  age_var = "age",
  sex_var = "sex",
  date_onset = "date_symptom_onset",
  date_exit = "date_exit_eff",
  outcome_var = "type_of_exit",
  outcome_levels = c("Recovered", "Died"),
  male_values = c("Male", "M", "Homme", "H"),
  hf_name_pattern = "HF_name_visited",
  hf_start_pattern = "date_start_HF_visited",
  hf_end_pattern = "date_end_HF_visited",
  disease_col = "#f2b0b0",
  incubation_col = "#ffe6cc",
  onset_col = "darkred",
  recovered_col = "#add8e6",
  died_col = "#7f7f7f",
  recovered_value = "Recovered",
  place_filter = shiny::reactiveVal(),
  time_filter = shiny::reactiveVal(),
  filter_info = shiny::reactiveVal(),
  filter_reset = shiny::reactiveVal()
)
}
\arguments{
\item{id}{Module id. Must match between \code{\link[=timeline_ui]{timeline_ui()}} and
\code{\link[=timeline_server]{timeline_server()}}.}

\item{title}{Header title for the card.}

\item{icon}{Icon shown next to the title. Defaults to a \link[bsicons:bsicons]{bsicons} glyph.}

\item{tooltip}{Optional hover-text shown next to the title.}

\item{facility_lab}{Label for the facility picker.}

\item{cases_lab}{Label for the cases picker.}

\item{label_lab}{Label for the y-axis identifier picker.}

\item{agesex_lab}{Label for the age/sex toggle.}

\item{incubation_lab}{Label for the incubation slider.}

\item{incubation_days, incubation_min, incubation_max}{Default, minimum and
maximum value (in days) of the supposed-incubation slider.}

\item{opts_btn_lab}{Label for the options popover / sidebar trigger.}

\item{full_screen}{Add a full-screen toggle to the card?}

\item{use_sidebar}{If \code{TRUE} (default), render options in a right-hand
\link[bslib:sidebar]{bslib::sidebar}; otherwise use a popover.}

\item{sidebar_title}{Title for the sidebar. Only used when
\code{use_sidebar = TRUE}.}

\item{sidebar_width}{Width of the sidebar in pixels. Default \code{300}.}

\item{df}{A data frame (or \code{\link[shiny:reactive]{shiny::reactive()}} returning one) of patient-
level linelist data, one row per case.}

\item{id_var}{Name of the case-id column (also the join key to the
structure-visit columns).}

\item{name_var, pid_var}{Columns offered in the y-axis "Identifiant" picker
(patient name and patient id).}

\item{age_var, sex_var}{Age and sex columns, used for the optional
\code{name (sex-age)} y-axis labels.}

\item{date_onset, date_exit}{Symptom-onset and exit date columns.}

\item{outcome_var}{Outcome column. Only rows whose value is in
\code{outcome_levels} are drawn.}

\item{outcome_levels}{Outcome values kept for the chart.}

\item{male_values}{Values of \code{sex_var} treated as male when building the
\code{sex-age} label.}

\item{hf_name_pattern, hf_start_pattern, hf_end_pattern}{Column-name stems
(matched with \code{\link[dplyr:contains]{dplyr::contains()}}) for the repeated health-structure
name / start-date / end-date fields (e.g. \code{HF_name_visited1},
\code{HF_name_visited2}, ...).}

\item{disease_col, incubation_col, onset_col, recovered_col, died_col}{Fill
colours for the disease bar, incubation window, onset marker, recovered
exit and died exit.}

\item{recovered_value}{Value of \code{outcome_var} denoting recovery (drawn with
\code{recovered_col}; any other kept level uses \code{died_col}).}

\item{place_filter}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when set, narrows \code{df}
to a clicked admin region. Receives values produced by
\code{epishiny::place_server()}.}

\item{time_filter}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when set, narrows \code{df} to
a clicked time period. Receives values produced by
\code{epishiny::time_server()}.}

\item{filter_info}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} holding the current filter-info
HTML string, kept in sync across modules.}

\item{filter_reset}{A \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} that, when bumped, signals the
module to clear its internal facility / cases selection.}
}
\value{
\code{timeline_ui()} returns a \link[bslib:card]{bslib::card}. \code{timeline_server()} returns
a \code{\link[shiny:reactive]{shiny::reactive()}} of the case ids currently shown, suitable for wiring
into sibling modules.
}
\description{
A Shiny module that draws one care-pathway timeline per case as a Highcharts
\code{xrange} chart: symptom onset to exit (recovery / death), the health
structures visited drawn as rectangles over the disease phase, and an
optional supposed-incubation window before onset. Follows the \code{epishiny}
module conventions so it composes inside a dashboard alongside the core
\code{time}, \code{place} and \code{person} modules.
}
\examples{
if (interactive()) {
  library(shiny)
  library(bslib)
  library(epishiny.timeline)

  # A minimal synthetic linelist: one row per case plus repeated
  # health-structure visit columns (HF_name_visited1, date_start_HF_visited1, ...)
  onset <- as.Date(c("2024-01-03", "2024-01-05", "2024-01-10"))
  ll <- data.frame(
    patient_name = c("Ada Lovelace", "Alan Turing", "Grace Hopper"),
    pid = c("P001", "P002", "P003"),
    age = c(34L, 41L, 28L),
    sex = c("Female", "Male", "Female"),
    date_symptom_onset = onset,
    date_exit_eff = onset + c(12, 8, 15),
    type_of_exit = c("Recovered", "Died", "Recovered"),
    HF_name_visited1 = c("City Hospital", "Health Post", "City Hospital"),
    date_start_HF_visited1 = onset + 1,
    date_end_HF_visited1 = onset + c(5, 3, 6),
    HF_name_visited2 = c("General Hospital", NA, "General Hospital"),
    date_start_HF_visited2 = c(onset[1] + 5, NA, onset[3] + 6),
    date_end_HF_visited2 = c(onset[1] + 12, NA, onset[3] + 15),
    stringsAsFactors = FALSE
  )

  # standalone single-module preview
  launch_timeline(ll)

  # or compose it inside a dashboard alongside epishiny's modules:
  # ui <- bslib::page_sidebar(
  #   sidebar = epishiny::filter_ui("filter", date_vars = "date_symptom_onset"),
  #   epishiny.timeline::timeline_ui("timeline")
  # )
  # server <- function(input, output, session) {
  #   app <- epishiny::filter_server("filter", df = ll,
  #                                  date_vars = "date_symptom_onset")
  #   epishiny.timeline::timeline_server("timeline", df = app$df,
  #                                      filter_info = app$filter_info)
  # }
  # shinyApp(ui, server)
}
}
